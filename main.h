#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <string.h>
#include <unistd.h>


#define BUFFER SIZE 1024;

int _printf(const char *format, ...);
int binary(int num);
int reverse(char *str);









#endif
